# FRETRUSH ギター練習リズムゲーム 外部仕様書 (v1 ドラフト)

## 1. 概要

**FRETRUSH** は、エレキギターの練習を目的とした、音ゲー(ギタドラ風)形式のアプリケーション。
画面上部から流れてくる譜面(ノーツ)に合わせて実際にギターを演奏し、マイクから
拾った音を解析して、音程・タイミングの精度を判定する。

- 対象ユーザー: 演奏スキルにあまり自信がない初心者〜中級者の自主練習用
- v1スコープ: **単音メロディの判定のみ**(和音判定は将来拡張)
- 譜面の作り方: **既存のGuitar Pro形式TAB譜ファイルのインポート**(mp3からの自動採譜は将来拡張)

画面イメージ(モックアップ): [FretRush Screens](https://claude.ai/code/artifact/4f717382-3d66-4ec6-b319-ee3c9f29a598)
(4章の各画面と、キャンバス上の各アートボードが対応。カッコ内の英名がアートボード名)

## 2. 動作環境

- ブラウザベースのWebアプリケーション(HTML5 Canvas + Web Audio API)
- サーバー不要、ローカル(クライアントサイド)で完結
- 対応ブラウザ: Chrome系の最新版を第一ターゲット
- 入力デバイス: マイク
  - v1の主な想定: USBカメラ付属マイクなど汎用USBマイク
  - オーディオインターフェース経由の入力にも対応する(遅延・精度の面で有利なため、環境があればそちらを推奨する旨をUI上で案内する)
  - マイク経由の入力は機材によって遅延・ノイズの度合いが変わるため、キャリブレーション機能(4.3節)で吸収する
- 対象楽器: エレキギター(v1ではレギュラーチューニング EADGBE 固定)

## 3. 機能一覧

1. TAB譜インポート機能
2. 譜面表示機能(ノーツがレーンを流れてくる)
3. マイク入力・ピッチ検出機能
4. 判定機能(音程・タイミング)
5. スコア/コンボ表示機能
6. リザルト表示機能
7. キャリブレーション機能(マイク音量チェック・レイテンシ補正)
8. スコア保存・自己ベスト管理機能(曲×難易度×再生速度ごとにベストスコアを記録)
9. BGM再生機能(TAB譜に含まれる音源情報を合成音で再生)
10. 再生速度調整機能(曲全体をゆっくり再生する練習用スロー再生)

## 4. 画面構成

```
タイトル画面
  └→ 譜面選択/インポート画面
        └→ キャリブレーション画面
              └→ プレイ画面
                    └→ リザルト画面 ──(リトライ/戻る)──→ 譜面選択 or プレイ画面
```

### 4.1 タイトル画面 (画面イメージ: `Title`)
- アプリ名表示、「はじめる」導線のみのシンプルな画面

### 4.2 譜面選択/インポート画面 (画面イメージ: `SongSelect`)
- ローカルのTAB譜ファイルを選択してインポート
- インポート済み譜面の一覧(曲名、長さ、難易度目安など)から選択してプレイ開始

### 4.3 キャリブレーション画面 (画面イメージ: `Calibration`)
- マイク入力レベルの確認(音量メーターを表示し、正しく音を拾えているか確認)
- レイテンシ補正値の設定(任意。手拍子やアタック音を鳴らして遅延を計測、または手動でオフセット調整)
- 再生速度の設定(無段階スライダー。曲・ノーツの流れる速さとBGMを連動してゆっくり再生できる。詳細は6章)

### 4.4 プレイ画面 (画面イメージ: `Main`)
- レーン: TAB譜と同じ並び順で、1弦を一番上・6弦を一番下にした6本の水平レーン
- ノーツ: 画面左側から出現し、右方向へ流れて判定ラインに向かう。TAB譜の「弦番号+フレット番号」を元にした音符オブジェクト
- 判定ライン: 画面右側に固定した縦のライン(=Perfectのタイミングを示す基準線)
- 弦名(EADGBE)のラベルは判定ラインのすぐ右側に表示し、どのレーンがどの弦かを判定ラインの位置で確認できるようにする
- リアルタイムにマイク入力からピッチを検出し、判定ラインに到達したノーツと音程・タイミングを比較して判定
- 判定結果(Perfect/Good/Miss等)をリアルタイムに表示
- コンボ数、現在のスコアを常時表示
- BGM(6章)を再生しながらプレイする。ノーツの流れる速さとBGMの再生速度は常に連動する

### 4.5 リザルト画面 (画面イメージ: `Result`)
- 総合スコア
- 判定内訳(Perfect/Good/Missそれぞれの数)
- 最大コンボ数
- 今回プレイした曲×難易度×再生速度における自己ベストとの比較(自己ベスト更新時は明示的に表示)
- 同じ曲について、難易度・再生速度ごとのベストスコア一覧(未挑戦の組み合わせも分かるように表示。等倍速を基本の一覧とし、スロー再生時の記録は別途参照できるようにする)
- リトライ / 譜面選択に戻る の導線

## 5. TAB譜データ仕様

TAB譜の事実上の業界標準である **Guitar Pro形式**(.gp3, .gp4, .gp5, .gpx, .gp)を採用する。
独自フォーマットは定義せず、オープンソースのTAB譜解析・描画ライブラリ **[alphaTab](https://alphatab.net/)**
(`@coderline/alphatab`)を用いてファイルを読み込む。

- インポート対象: Guitar Pro形式ファイル(.gp3〜.gp5, .gpx, .gp)。alphaTabはMusicXMLの読み込みにも対応する
- メリット:
  - 独自フォーマットを新規に設計・実装する必要がない
  - インターネット上に既に存在する大量のGuitar Pro形式TAB譜をそのまま利用できる
  - alphaTabがファイル内のノート情報(弦番号・フレット番号・開始タイミング・長さ・BPM・チューニング等)を
    構造化データとして提供するため、そこから判定用データを組み立てられる
- v1で利用するノート情報: 開始タイミング、弦番号、フレット番号(音の長さは表示用途が主で、判定は主に始点で行う)
- 自作の練習用譜面を書きたい場合は、alphaTab付属のテキスト記法「alphaTex」で作成し、Guitar Pro形式と
  同様に読み込む選択肢も用意する(任意)
- BGM再生(6章)には、mp3等の音声ファイルではなく、Guitar Pro形式ファイルに含まれる演奏情報を
  alphaTab付属のシンセサイザー「alphaSynth」で合成音として再生する方式を採る。曲ごとに別途
  音源ファイルを用意する必要がない

## 6. BGM再生・再生速度仕様

- BGMは5章の通り alphaSynth による合成音再生とする。実際の楽曲音源(mp3等)は使用しない
- 再生速度は **50%〜100%の範囲で無段階に調整**できるようにする(v1では等倍速より速くする
  機能は対象外。「ゆっくり練習する」用途に絞る)
- ノーツが流れる速さとBGMの再生速度は常に連動して変化させる(選んだ速度でノーツ間隔・
  BGMのテンポの両方が一緒に伸び縮みする)
- BGMはMIDI相当の演奏情報を都度合成しているため、再生速度を変えてもテンポが変わるだけで
  **音程(ピッチ)は変化しない**(一般的な音声ファイルの再生速度変更と異なり、タイムストレッチ
  処理を別途行う必要がない)
- 判定の許容誤差(7.1節の表)はミリ秒・セントという実時間・実音程の単位のまま適用する。
  速度を落とすとノーツ同士の間隔が実時間で広がるため、結果として体感的な難易度は下がるが、
  判定基準の閾値自体は変更しない
- 再生速度はキャリブレーション画面(4.3節)で選択し、プレイ中の曲を通して固定する
  (曲の再生中に動的に速度を変える操作はv1のスコープ外とする)

## 7. 音声解析仕様

- マイク入力: `getUserMedia` でPCMストリームを取得
- ピッチ検出アルゴリズム: YIN法(または自己相関法)による基本周波数推定
  - v1は単音検出のみを対象とし、和音(複数基音)の分離は行わない
- 検出した周波数を音名/フレット位置に変換し、判定対象ノーツの期待値と比較

## 8. 判定仕様

- 判定要素: ①音程の一致度、②タイミングのズレ
- 判定ランク自体は Perfect / Good / Miss の3段階で表示するが、**その閾値(音程許容誤差・タイミング許容誤差)を
  ユーザーが選択できる**ようにする
  - プリセットとして「かなり緩め〜かなり厳しめ」の5段階を用意する
  - 加えて、音程許容誤差(セント単位)・タイミング許容誤差(ミリ秒単位)を個別に無段階で調整できる詳細設定も用意する
  - デフォルトは緩め寄りの設定とし、練習の習熟度に応じて厳しくしていける想定
- マイク入力〜解析〜判定までの処理遅延を見込み、キャリブレーションで得たオフセット値を判定タイミングに適用する
- 再生速度(6章)を落としても、判定の許容誤差(実時間・実音程)自体は変えない

### 8.1 難易度プリセット別の許容誤差(初期値)

各ノーツの正解タイミング(判定ラインに到達する時刻)からのズレを基準に判定する。
以下は**初期値の仮設定**であり、実プレイでのテストを踏まえてチューニングする前提とする。

| プリセット | タイミング: Perfect | タイミング: Good | タイミング: Miss | 音程: Perfect | 音程: Good |
|---|---|---|---|---|---|
| かなり緩め | ±80ms以内 | ±150ms以内 | それ以外 | ±50セント以内 | ±80セント以内 |
| 緩め | ±60ms以内 | ±120ms以内 | それ以外 | ±35セント以内 | ±60セント以内 |
| 標準(デフォルト) | ±40ms以内 | ±90ms以内 | それ以外 | ±25セント以内 | ±45セント以内 |
| 厳しめ | ±25ms以内 | ±60ms以内 | それ以外 | ±15セント以内 | ±30セント以内 |
| かなり厳しめ | ±15ms以内 | ±40ms以内 | それ以外 | ±10セント以内 | ±20セント以内 |

- 100セント = 半音1つ分。Goodの音程許容誤差は半音の半分未満に収め、隣接する音と混同しないようにする
- 判定ランクは「タイミング判定」と「音程判定」のうち**厳しい方の結果を採用**する(例: タイミングはPerfect範囲でも音程がGood範囲ならGood判定)
- タイミングが最も緩いプリセットのMiss境界(150ms)を超えて音を検出できなかった場合、またはノーツ通過時に該当する音が全く検出されなかった場合はMissとする
- マイク入力〜ピッチ検出処理自体に数十ms程度の遅延が生じ得るため(7章)、タイミング許容誤差は一般的な音ゲーよりやや広めに設定している
- 音程許容誤差(セント単位)・タイミング許容誤差(ミリ秒単位)を個別に無段階調整する詳細設定では、上表の数値を初期値として個別に変更できるようにする

## 9. スコア計算仕様

- 満点(全ノーツPerfect時のスコア)を **1,000,000点** に固定し、曲のノーツ数によらずスコアを比較しやすくする
- 1ノーツあたりの基礎点 = `1,000,000 ÷ 総ノーツ数`
- 各ノーツの獲得点 = `基礎点 × 判定係数 × コンボ倍率`
  - 判定係数: Perfect = 1.0 / Good = 0.5 / Miss = 0
  - コンボ倍率: `1 + min(そのノーツ時点の連続Perfect数 ÷ 100, 0.5)`(最大1.5倍。100連続Perfectで頭打ち。Good/Missでコンボは0にリセット)
- 曲の総獲得点 = 各ノーツの獲得点の合計(端数は四捨五入)
- ランク(S/A/B/C等)は総獲得点に対する割合で決定する(例: S = 95%以上、A = 85%以上 … 具体的な閾値は今後のチューニング対象)
- 上記は**初期値の仮設計**であり、実プレイでのテストを踏まえて係数・倍率を調整する前提とする

## 10. スコア保存仕様

上達を実感できるよう、プレイのたびにスコアを記録し、自己ベストとの比較を提示する。

- 記録単位: **曲 × 判定の厳しさ(難易度プリセット/詳細設定) × 再生速度(6章)** ごとに1レコード
  - 判定基準・再生速度を変えると難度が変わるため、組み合わせごとに別のベストスコアとして管理する
  - 詳細設定でプリセットから外れた場合の扱い(専用レコードにするか、最も近いプリセットに丸めるか)は実装時に検討する
  - 再生速度は無段階のため、記録上どの粒度で区切るか(例: 5%刻みに丸める等)は実装時に検討する。
    等倍速(100%)は必ず独立したレコードとして扱う
- 1レコードが持つ情報: ベストスコア、ランク、最大コンボ、正解率、判定内訳、記録日時
- 記録タイミング: プレイ終了時、既存のベストスコアを上回った場合に更新する
- 参照タイミング: リザルト画面で今回のスコアと自己ベストを比較表示、および同曲の難易度・再生速度別ベストスコア一覧を表示(4.5節)
- 保存先: v1はローカル(ブラウザストレージ)。サーバー同期は行わない

## 11. 非機能要件

- 判定処理はブラウザ内でリアルタイムに完結させる(サーバーへの音声送信は行わない)
- マイク〜処理〜表示までの遅延はキャリブレーションである程度吸収できるようにする
- 譜面データ・設定・スコア記録はローカル(ブラウザストレージ等)に保存し、次回起動時も利用可能にする

## 12. v1スコープ外(将来拡張)

- コード(和音)判定
- mp3等の音源ファイルからのTAB譜自動生成
- 変則チューニング対応
- 曲そのものの難易度別譜面、オンラインランキング等
- 等倍速より速い再生速度、プレイ中の動的な速度変更
