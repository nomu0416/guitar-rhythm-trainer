# ギター練習リズムゲーム 外部仕様書 (v1 ドラフト)

## 1. 概要

エレキギターの練習を目的とした、音ゲー(ギタドラ風)形式のアプリケーション。
画面上部から流れてくる譜面(ノーツ)に合わせて実際にギターを演奏し、マイクから
拾った音を解析して、音程・タイミングの精度を判定する。

- 対象ユーザー: 演奏スキルにあまり自信がない初心者〜中級者の自主練習用
- v1スコープ: **単音メロディの判定のみ**(和音判定は将来拡張)
- 譜面の作り方: **TAB譜ファイルのインポート**(mp3からの自動採譜は将来拡張)

## 2. 動作環境

- ブラウザベースのWebアプリケーション(HTML5 Canvas + Web Audio API)
- サーバー不要、ローカル(クライアントサイド)で完結
- 対応ブラウザ: Chrome系の最新版を第一ターゲット
- 入力デバイス: マイク(内蔵マイク、または外部マイク/オーディオインターフェース経由)
- 対象楽器: エレキギター(v1ではレギュラーチューニング EADGBE を前提)

## 3. 機能一覧

1. TAB譜インポート機能
2. 譜面表示機能(ノーツがレーンを流れてくる)
3. マイク入力・ピッチ検出機能
4. 判定機能(音程・タイミング)
5. スコア/コンボ表示機能
6. リザルト表示機能
7. キャリブレーション機能(マイク音量チェック・レイテンシ補正)

## 4. 画面構成

```
タイトル画面
  └→ 譜面選択/インポート画面
        └→ キャリブレーション画面
              └→ プレイ画面
                    └→ リザルト画面 ──(リトライ/戻る)──→ 譜面選択 or プレイ画面
```

### 4.1 タイトル画面
- アプリ名表示、「はじめる」導線のみのシンプルな画面

### 4.2 譜面選択/インポート画面
- ローカルのTAB譜ファイルを選択してインポート
- インポート済み譜面の一覧(曲名、長さ、難易度目安など)から選択してプレイ開始

### 4.3 キャリブレーション画面
- マイク入力レベルの確認(音量メーターを表示し、正しく音を拾えているか確認)
- レイテンシ補正値の設定(任意。手拍子やアタック音を鳴らして遅延を計測、または手動でオフセット調整)

### 4.4 プレイ画面
- レーン: 弦ごとに6レーン(1〜6弦)、画面奥/上から判定ラインに向かってノーツが流れてくる
- ノーツ: TAB譜の「弦番号+フレット番号」を元にした音符オブジェクト
- 判定ライン: 画面下部(または任意の固定位置)
- リアルタイムにマイク入力からピッチを検出し、判定ラインに到達したノーツと音程・タイミングを比較して判定
- 判定結果(Perfect/Good/Miss等)をリアルタイムに表示
- コンボ数、現在のスコアを常時表示

### 4.5 リザルト画面
- 総合スコア
- 判定内訳(Perfect/Good/Missそれぞれの数)
- 最大コンボ数
- リトライ / 譜面選択に戻る の導線

## 5. TAB譜データ仕様

- v1では独自定義のJSONフォーマットを想定(既存フォーマットへの対応は将来拡張)
- 1ノートが持つ情報:
  - 開始タイミング(曲頭からのミリ秒、またはBPM+拍単位)
  - 弦番号(1〜6)
  - フレット番号(0=開放弦)
  - 音の長さ(任意、判定には主に始点のみ使用する想定)
- 譜面全体のメタデータ:
  - 曲名、BPM、チューニング(v1は固定でEADGBEのみサポート)、音源ファイルの有無

## 6. 音声解析仕様

- マイク入力: `getUserMedia` でPCMストリームを取得
- ピッチ検出アルゴリズム: YIN法(または自己相関法)による基本周波数推定
  - v1は単音検出のみを対象とし、和音(複数基音)の分離は行わない
- 検出した周波数を音名/フレット位置に変換し、判定対象ノーツの期待値と比較

## 7. 判定仕様

- 判定要素: ①音程の一致度、②タイミングのズレ
- 判定ランク: Perfect / Good / Miss の3段階(仮。誤差の閾値は今後のチューニング対象)
- マイク入力〜解析〜判定までの処理遅延を見込み、キャリブレーションで得たオフセット値を判定タイミングに適用する

## 8. 非機能要件

- 判定処理はブラウザ内でリアルタイムに完結させる(サーバーへの音声送信は行わない)
- マイク〜処理〜表示までの遅延はキャリブレーションである程度吸収できるようにする
- 譜面データ・設定はローカル(ブラウザストレージ等)に保存し、次回起動時も利用可能にする

## 9. v1スコープ外(将来拡張)

- コード(和音)判定
- mp3等の音源ファイルからのTAB譜自動生成
- 既存TAB譜フォーマット(Guitar Pro形式など)のインポート対応
- 変則チューニング対応
- 難易度別譜面、オンラインランキング等
